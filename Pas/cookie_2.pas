Const   maxn = 600001;
Var     n       :       longint;
        ke,next :       array [0..maxn] of longint;

begin
     assign(input,'cookie.inp'); reset(input);
     assign(output,'cookie.out'); rewrite(output);

end.