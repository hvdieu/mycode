const inp = 'ab.inp';
      out = 'ab.out';
      maxn = 100;
var   n :longint;
      a : array [1..maxn] of longint;
 begin
      assign(input,inp); reset(input);
      assign(output,out);rewrite(output);

 end.