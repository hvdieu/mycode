#include <bits/stdc++.h>

using namespace std;

#define bit(x, i) ((x >> i) & 1)

int a[4], n;
long long ans, f[15][2][2][2][2][2][2][2];

long long cal(int i, bool ok1, bool ok2, bool ok3, bool ok4, bool l1, bool l2, bool l3) {
    if (f[])
}

int main() {
    for(int i = 0; i < 3; i++) cin >> a[i];
    sort(a, a+4);
    n = int(log(a[3]) / log(2));

}
